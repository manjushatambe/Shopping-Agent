services:
  - type: web
    name: shopping-ai-backend
    env: python
    buildCommand: "pip install -r requirements.txt && playwright install"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"